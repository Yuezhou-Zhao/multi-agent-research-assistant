venv/
__pycache__/
*.pyc
.env
.pytest_cache/
*.egg-info/
.DS_Store
faiss_index/
*.faiss
index/
.chainlit/
chainlit.md
.files/

# In-progress labeling artifacts — user manages these outside git
experiments/results/cascade_labels_draft.csv

# Docker build transcript (tee output, regenerable)
build.log




# LoRA data-prep generated artifacts (regenerable; the human-reviewed
# eval_spotcheck.csv is the exception — force-add it when filled in)
experiments/lora_supervisor/data/

# LoRA training artifacts: adapter + checkpoints (~86MB, regenerable
# from committed train_set.jsonl via train_lora.py). Keep the repo lean.
experiments/lora_supervisor/train/
