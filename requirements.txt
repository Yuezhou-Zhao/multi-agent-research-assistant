# Core agent framework
langgraph>=0.2.0
langchain-openai>=0.1.0

# API + serving
fastapi>=0.110.0
uvicorn>=0.29.0
chainlit>=1.1.0

# RAG pipeline
faiss-cpu>=1.8.0
rank-bm25>=0.2.2
FlagEmbedding>=1.2.0        # BGE-reranker-v2-m3
sentence-transformers>=2.7.0
pymupdf4llm>=0.0.5
# FlagEmbedding 1.4.0's reranker calls a tokenizer API (prepare_for_model)
# that transformers>=5.0 removed; pinned below or FlagReranker crashes.
transformers==4.46.3

# External APIs
arxiv>=2.1.0
tavily-python>=0.3.0

# MCP — serves the Web sub-agent's tool set (mcp_servers/web_research.py)
# and the client the graph calls it through. Chainlit already pulls this in
# transitively; declared explicitly because the agent now depends on it
# directly, so a Chainlit bump can't silently drop it. The floor is where
# structuredContent tool results landed; requirements.lock pins the tested
# 1.28.1.
mcp>=1.10.0

# Gamma guardrail (survival-function fit)
scipy>=1.10.0
numpy>=1.24.0

# Infrastructure
pydantic>=2.0.0
python-dotenv>=1.0.0
aiohttp>=3.9.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
